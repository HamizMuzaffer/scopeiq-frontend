/**
 * Scoping calculations and timeline utilities specific to the How It Works module
 */

export const howItWorksHelper = {
  estimateDaysNeeded: (stepsCount: number): number => {
    return stepsCount * 2.5;
  }
};
