import { AboutModule } from '@/modules/about';
export const metadata = {
  title: 'About Us | ScopeIQ',
  description: 'Learn about the design principles, corporate history, and system values behind the ScopeIQ Operational Scoper.',
};

export default function About() {
  return <AboutModule />;
}
